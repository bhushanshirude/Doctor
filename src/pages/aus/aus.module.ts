import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AusPage } from './aus';

@NgModule({
  declarations: [
    AusPage,
  ],
  imports: [
    IonicPageModule.forChild(AusPage),
  ],
})
export class AusPageModule {}
